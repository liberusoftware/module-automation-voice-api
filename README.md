# Voice API

One-to-one HTTP API adapter for `module-automation-voice`. Routes are versioned under `/api/v1/automation/voice`, require authenticated team context, and delegate domain mutations to the core package.

